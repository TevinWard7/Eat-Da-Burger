<div class="row text-center">

    <div class="col-5">
        <ul>
            {{#each burgers}}
            {{#unless devoured}}
            <li>"{{burger_name}}"</li>
            <p>🍔</p>
            <button data-id="{{id}}" class="eat">devour</button>
            {{/unless}}
            {{/each}}
        </ul>
    </div>

    <div class="col-2">

    </div>

    <div class="col-5">
        <ul>
            {{#each burgers}}
            {{#if devoured}}
            <li>{{burger_name}} 💯</li>
            {{/if}}
            {{/each}}
        </ul>
    </div>

</div>

<div class="row text-center">
    <div class="col">
        <label>Make a burger</label>
    </div>
</div>

<div class="row text-center">
    <div class="col">
        <form action="/" method="POST">
            <input type="text" name="burger">
            <input type="submit" value="Submit">
        </form>
    </div>
</div>