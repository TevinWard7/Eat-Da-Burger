<div class="row">

    <div class="col-5">
        <ul>
            {{#each burgers}}
            {{#unless devoured}}
            <li>{{burger_name}}</li>
            <button id="eat">devour</button>
            {{/unless}}
            {{/each}}
        </ul>
    </div>

    <div class="col-2">

    </div>

    <div class="col-5">
        <ul>
            {{#each burgers}}
            {{#if devoured}}
            <li>{{burger_name}}</li>
            {{/if}}
            {{/each}}
        </ul>
    </div>

</div>